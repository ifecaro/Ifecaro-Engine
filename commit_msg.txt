feat(toast): add smooth fade-in/out animations

Implement custom toast animation using Tailwind utility classes `toast-animate-in` and `toast-animate-out`.

Changes:
- Updated `src/components/toast.rs`
  • Use `toast-animate-in` on mount for entry animation
  • Start visible immediately to avoid flash
  • Apply `toast-animate-out` for exit animation
  • Sync removal delay to animation duration
- Added new test `tests/toast_animation_css_tests.rs` to ensure CSS classes exist
- Adjusted timeouts in auto-dismiss logic

All unit tests pass.
