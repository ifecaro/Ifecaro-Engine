server {
    listen 80;
    server_name _;

    # Avoid 400 Request Header Or Cookie Too Large when browser cookies grow.
    client_header_buffer_size 16k;
    large_client_header_buffers 8 32k;

    root /usr/share/nginx/html;
    index index.html;

    # Temporarily disable SPA fallback to verify routing behavior.
    location / {
        add_header X-Ifecaro-Debug-Route "static-or-404" always;
        try_files $uri $uri/ =404;
    }

    location = /staging {
        return 302 /staging/;
    }

    location = /version.json {
        add_header Cache-Control "no-store" always;
        try_files /version.json =404;
    }
    location = /db {
        add_header X-Ifecaro-Debug-Route "db-canonical-redirect" always;
        return 302 /db/;
    }

    location = /db/__debug {
        default_type application/json;
        add_header Cache-Control "no-store" always;
        add_header X-Ifecaro-Debug-Route "db-debug-endpoint" always;
        return 200 '{"request_uri":"$request_uri","uri":"$uri","host":"$host","http_x_forwarded_proto":"$http_x_forwarded_proto","remote_addr":"$remote_addr"}';
    }

    # PocketBase admin UI may navigate to /_ from script/runtime; keep it under /db.
    location = /_ {
        add_header X-Ifecaro-Debug-Route "root-admin-redirect-to-db" always;
        return 302 /db/_;
    }

    location ^~ /_/ {
        add_header X-Ifecaro-Debug-Route "root-admin-prefix-redirect-to-db" always;
        return 302 /db$uri$is_args$args;
    }

    # PocketBase is published on host port 18090 for staging mode.
    # /db/... -> /...
    location ^~ /db/ {
        proxy_http_version 1.1;
        add_header X-Ifecaro-Debug-Route "db-proxy" always;
        add_header X-Ifecaro-Upstream-Location $upstream_http_location always;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Prefix /db;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_redirect ~^(/.*)$ /db$1;
        proxy_redirect ~^https?://[^/]+(/.*)$ /db$1;
        rewrite ^/db(/.*)$ $1 break;
        proxy_pass http://172.17.0.1:18090;
    }
}
